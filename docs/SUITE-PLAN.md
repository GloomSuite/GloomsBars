# Gloom Suite plan — MOVED

The Gloom Suite unification plan now lives in its home of record, the **GloomsHub** repo, so it
can't drift from the other addons' copies:

- **`~/GloomsHub/docs/SUITE-PLAN.md`** — the architecture + phased plan + locked decisions
- **`~/GloomsHub/docs/SUITE-STATE.md`** — where the effort stands right now (read first)
- **`~/GloomsHub/docs/CONTRACTS.md`** — shared runtime contracts (tokens, tab API, resolver)

This pointer stays here only so a GB-only session finds its way to the Hub. Do not re-add the
full plan text to this repo — one home per fact.
