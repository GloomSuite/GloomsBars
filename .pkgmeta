# BigWigs packager config. Publishes to GitHub Releases ONLY (no CurseForge/
# Wago) — same model as GloomsBuildBarn/GloomsAuras. WoWUp installs + auto-
# updates from those releases via the repo URL.

package-as: GloomsBars

enable-nolib-creation: no

# Embedded libraries. Fetched into Libs/ at release build time only — NOT
# committed to the repo (gitignored). Locally, Libs/ is present in the working
# copy. Same model + URLs as GloomsAuras. LDB + LibDBIcon dropped with Phase C
# — the minimap launcher (and those libs) moved to GloomsHub, the hard
# dependency. (LibSharedMedia stays standalone-consumed for now.)
externals:
  Libs/LibStub: https://repos.wowace.com/wow/libstub/trunk
  Libs/CallbackHandler-1.0: https://repos.wowace.com/wow/callbackhandler/trunk/CallbackHandler-1.0

# Keep repo-management files out of the packaged addon zip.
ignore:
  - README.md
  - CLAUDE.md
  - .github
  - .gitignore
  - .pkgmeta
  - docs
  - tools
